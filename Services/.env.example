# Copy to Services/.env and fill in. Docker Compose reads it from
# the directory of the compose file.
MONGO_ROOT_USERNAME=
MONGO_ROOT_PASSWORD=

# Node-RED editor. Generate the hash with:  node-red admin hash-pw
NODERED_ADMIN_USER=
NODERED_ADMIN_PASSWORD_HASH=
